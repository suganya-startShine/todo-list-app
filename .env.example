# Database Configuration
DATABASE_URL=postgresql://username:password@host:port/database_name

# For platforms like Supabase, Neon, or ElephantSQL
# DATABASE_URL=postgresql://user:pass@host.region.provider.com:5432/dbname

# Secret Key
SECRET_KEY=your-super-secret-key-here-change-this

# Optional: Individual DB parameters (fallback)
DB_NAME=todo_db
DB_USER=todo_user
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
```

### 5. **Setup Instructions for Deployment:**

#### **For Vercel + External Database (Recommended):**

1. **Get a PostgreSQL Database:**
   - Use [Supabase](https://supabase.com/) (Free tier)
   - Use [Neon](https://neon.tech/) (Free tier)
   - Use [ElephantSQL](https://www.elephantsql.com/) (Free tier)
   - Use [Railway](https://railway.app/) (Free tier)

2. **Get Your Database URL:**
```
   postgresql://username:password@host:port/database_name